version: '3.8'

services:

  lms:
    build: .
    container_name: lms-app
    ports:
      - "9999:9999"
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: lms-redis
    ports:
      - "6379:6379"